
// The file is read-only, but we can change the implementation in other files.
// We'll add a wrapper component that enhances the SegmentsTable
import { TableSortableWrapper } from "./TableSortableWrapper";

export default TableSortableWrapper;
